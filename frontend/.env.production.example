# AutoPilot AI — frontend production environment.
# Set this in your host's environment UI (Vercel → Project → Settings →
# Environment Variables). It is baked in at build time, so changing it requires
# a redeploy, not just a restart.
#
# ⚠ The deployed instance has NO AUTHENTICATION: anyone with the URL can read and
#   write everything in it. See docs/COMPLETION_PLAN.md §3.

# Public base URL of the deployed backend (no trailing slash).
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
